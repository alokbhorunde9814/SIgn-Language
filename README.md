# ASL Interpreter Frontend

## Setup Instructions

1. Install dependencies:
```bash
npm install
Start development server:

bash
Copy
npm start
Build for production:

bash
Copy
npm run build
Project Structure
Camera feed with real-time processing

Text input with clear functionality

Dual response panels for sign and text interpretation

Bootstrap-powered responsive layout

Copy

To use this setup:

1. Create the folder structure as shown above
2. Copy all these files into their respective locations
3. Move your existing images to public/assets/images/
4. Run npm install in the frontend directory
5. Start the development server with npm start

The frontend will be available at http://localhost:3000

To connect with your backend/ML model